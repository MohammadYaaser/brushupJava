package com.java.brushup.java14;

public class SwitchExpressions {
    /*String result = switch("M")=> {
        case "M", "W", "F" -> System.out.println("MWF");
        case "T", "TH", "S" -> "TTS";
        default -> {
            if("M".isEmpty())
                yield "Please insert a valid day.";
            else
                yield "Looks like a Sunday.";
        }

    };*/
}
